# Seed data file - Empty by default
# Add your own seed data here if needed for development/testing
# All production data should be entered through the application UI

# Example structure (commented out):
# mutation CreateSeedData @transaction @auth(level: PUBLIC) {
#   inventory_insertMany(data: [])
#   sales_insertMany(data: [])
#   prescriptions_insertMany(data: [])
# }